// Public API keys — safe to commit (these are client-facing keys by design)
const CONFIG = {
  web3formsKey: '',  // fill in after Web3Forms signup (Phase G)
  hcaptchaSiteKey: '', // fill in after hCaptcha signup (Phase G)
};
